1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```

3. If app gets stuck or not updating the changes in the app run:

   ```bash
   npx expo start -c
   ```
 
